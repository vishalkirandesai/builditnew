// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package biz.buildit.web.we;

import biz.buildit.main.PlantHireRequest;
import biz.buildit.main.PurchaseOrder;
import biz.buildit.repository.PlantHireRequestRepository;
import biz.buildit.repository.PlantRepository;
import biz.buildit.repository.SiteEngineerRepository;
import biz.buildit.repository.WorksEngineerRepository;
import biz.buildit.util.Approval;
import biz.buildit.web.we.PHRWEController;
import java.io.UnsupportedEncodingException;
import java.util.Arrays;
import javax.servlet.http.HttpServletRequest;
import org.joda.time.format.DateTimeFormat;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect PHRWEController_Roo_Controller {
    
    @Autowired
    PlantHireRequestRepository PHRWEController.plantHireRequestRepository;
    
    @Autowired
    PlantRepository PHRWEController.plantRepository;
    
    @Autowired
    SiteEngineerRepository PHRWEController.siteEngineerRepository;
    
    @Autowired
    WorksEngineerRepository PHRWEController.worksEngineerRepository;
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String PHRWEController.show(@PathVariable("id") Long id, Model uiModel) {
        addDateTimeFormatPatterns(uiModel);
        uiModel.addAttribute("planthirerequest", plantHireRequestRepository.findOne(id));
        uiModel.addAttribute("itemId", id);
        return "we/phrs/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String PHRWEController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("planthirerequests", plantHireRequestRepository.findAll(new org.springframework.data.domain.PageRequest(firstResult / sizeNo, sizeNo)).getContent());
            float nrOfPages = (float) plantHireRequestRepository.count() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("planthirerequests", plantHireRequestRepository.findAll());
        }
        addDateTimeFormatPatterns(uiModel);
        return "we/phrs/list";
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String PHRWEController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        populateEditForm(uiModel, plantHireRequestRepository.findOne(id));
        return "we/phrs/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String PHRWEController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        PlantHireRequest plantHireRequest = plantHireRequestRepository.findOne(id);
        plantHireRequestRepository.delete(plantHireRequest);
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/we/phrs";
    }
    
    void PHRWEController.addDateTimeFormatPatterns(Model uiModel) {
        uiModel.addAttribute("plantHireRequest_startdate_date_format", DateTimeFormat.patternForStyle("M-", LocaleContextHolder.getLocale()));
        uiModel.addAttribute("plantHireRequest_enddate_date_format", DateTimeFormat.patternForStyle("M-", LocaleContextHolder.getLocale()));
        uiModel.addAttribute("plantHireRequest_extensiondate_date_format", DateTimeFormat.patternForStyle("M-", LocaleContextHolder.getLocale()));
    }
    
    void PHRWEController.populateEditForm(Model uiModel, PlantHireRequest plantHireRequest) {
        uiModel.addAttribute("plantHireRequest", plantHireRequest);
        addDateTimeFormatPatterns(uiModel);
        uiModel.addAttribute("plants", plantRepository.findAll());
        uiModel.addAttribute("purchaseorders", PurchaseOrder.findAllPurchaseOrders());
        uiModel.addAttribute("siteengineers", siteEngineerRepository.findAll());
        uiModel.addAttribute("worksengineers", worksEngineerRepository.findAll());
        uiModel.addAttribute("approvals", Arrays.asList(Approval.values()));
    }
    
    String PHRWEController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
